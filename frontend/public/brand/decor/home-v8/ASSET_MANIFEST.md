# Home V8 Asset Prep Manifest

This folder is for the next Home implementation pass. It is intentionally asset-first: do not start by tuning CSS shadows.

## Reference Assets

- `home-v8-final-target-desktop.png`
  - Copy of the approved desktop target mockup.
  - Use for layout, shadow strength, notebook color, tab spacing, and overall first impression.
  - This is a reference image, not a production UI background.

- `home-v8-shiori-selected-reference.png`
  - Copy of the approved Shiori reading-peek reference.
  - Use only as the pose/visibility reference.

## Required Production Asset

- `home-v8-notebook-tabs-grounded-plate.png`

Current production plate. It is a true transparent PNG containing notebook + tabs + tape + bookmark + emboss + baked contact shadow, with no note, CTA, Shiori, desk background, app toolbar, or baked text.

Generated by:

- `references/mockups/home-v8-prep/build_home_v8_plate_candidate.py`

Validated by:

- `references/mockups/home-v8-prep/validate_home_v8_plate.py`

Status:

- Passes alpha/border-residue validation.
- Wired into Home as the notebook plate.
- Browser check confirmed the old v7 plate URL is not requested and `.home-v4-notebook::before/::after` shadow pseudo-elements are not active.
- The first Claude-wired v8 candidate failed visual review because the baked shadow read as a broad support slab and the bookmark ribbon center was cut out. The generator now preserves the ribbon and builds shadow only from the notebook/tab bottom and right contact edges.
- A follow-up visual pass strengthened the contact-edge shadow after the deployed view showed almost no grounding. The current generator keeps the contact-only mask, thickens it slightly, and uses stronger soft falloff so the book and tabs read as resting on the table without returning to a rectangular support slab.

Prepared tab measurements:

- plate size: 1538x1247
- yellow: `left:31.73%; top:80.03%; width:12.61%; height:7.54%`
- coral: `left:45.64%; top:79.87%; width:12.68%; height:7.70%`
- blue: `left:59.75%; top:80.11%; width:12.81%; height:7.46%`

## Hard Rules

- No CSS-only shadow solution.
- No full-plate shadow PNG layered behind the old plate.
- No checkerboard/RGB fake transparency.
- No white/gray matte residue around notebook or tabs.
- Recalculate hit zones from the final plate.
